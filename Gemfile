source "https://rubygems.org"

# Load this gem's runtime + development dependencies from the gemspec.
gemspec

group :development do
  # Style. Matches the host conventions used in rbrun.
  gem "rubocop-rails-omakase", require: false
end
